name: Docker - Actions Service

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-template:
    uses: ./.github/workflows/docker-image-template.yml
    with:
      dockerfile: ./services/actions-service/Dockerfile
      image_name: ghcr.io/${{ github.repository_owner }}/${{ github.event.repository.name }}:actions-service-latest
      context: ./services/actions-service/
      push_on_main: true
